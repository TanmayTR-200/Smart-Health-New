# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# ML/AI
# Prophet is installed locally for time-series forecasting. On Render's free
# tier, pip install of prophet OOMs (compiles Stan C++ models, needs >2GB).
# The code auto-detects: if Prophet is importable it uses it (method="prophet"),
# otherwise it falls back to moving average / seasonal trend.
# prophet==1.1.5
# cmdstanpy>=1.0.4,<1.3  # Pinned: prophet 1.1.5 breaks with cmdstanpy 1.3+
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4

# Google Gemini API
google-generativeai>=0.8.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Testing
pytest>=7.0
